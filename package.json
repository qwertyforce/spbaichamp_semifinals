{
  "name": "spb-ai-champ",
  "version": "0.2.0",
  "main": "main.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/main.js",
    "start2": "node build/main.js"
  },
  "devDependencies": {
    "@types/node": "^16.9.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "source-map-support": "^0.5.20"
  }
}
